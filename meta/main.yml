galaxy_info:
  role_name: development_essentials
  author: Christian Günther
  description: Installs development essentials
  version: 0.0.1
  platforms:
    - name: Ubuntu
  galaxy_tags:
    - development
dependencies:

  - role: "geerlingguy.docker"
    become: yes
    vars:
      docker_users:
        - "{{ ansible_user_id }}"

  - role: "koichirok.google_chrome"
    become: yes